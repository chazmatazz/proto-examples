(all
 (def fib (n) 
   (if (<= n 1)
     0
     (fib (- n 1))
     )
   )
 )

(def fib-blue(n) 
  (blue (fib n))
  )

;; DOES NOT WORK!!!
;; run with
;; proto -n 5 -sv -l -m -w -s 0.01 "(fib-blue 1)"
